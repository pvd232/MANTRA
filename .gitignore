# ---------- Python ----------
__pycache__/
*.py[cod]
*.so
*.egg-info/
.mypy_cache/
.pytype/
.ipynb_checkpoints/

# ---------- OS / Editors ----------
.DS_Store
Thumbs.db
.vscode/
.idea/

# ---------- Envs ----------
.venv/
venv/
.env
.env.*
.conda/
conda-meta/
# auto-generated by `conda env create`
configs/condaenv.*.requirements.txt

# ---------- Logs ----------
logs/
*.log

# ---------- Data (downloaded / derived) ----------
# raw sources fetched from the internet (Figshare, etc.)
data/raw/**
!data/raw/.keep

# intermediate AnnData, QC outputs, etc.
data/interim/**
!data/interim/.keep

# external genetics slices you may cache locally
data/smr/**
!data/smr/.keep

# any local model artifacts (if produced)
models/**
!models/.keep
checkpoints/**
!checkpoints/.keep

# pipeline outputs/figures/metrics
out/**
!out/.keep

# ---------- Temp / Download staging ----------
tmp/
tmp_*/
**/tmp_downloads/
**/*.aria2
**/*.aria2.resume
**/*.part
**/*.crc
**/*.tmp
**/*.download
aria2*/  # if you create a dedicated aria2 cache dir

# ---------- GCP / gsutil local config (user-specific) ----------
.boto

# ---------- Build / Dist ----------
build/
dist/
*.egg

# ---------- Maps / HTML (optional; keep if generated) ----------
map/*.html
map/*.png
